spring:
  application:
    name: service-consumer

server:
  port: 9002

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/

#hystrix
feign:
  hystrix:
    enabled: true